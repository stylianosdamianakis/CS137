#ifndef FUNCTIONS_H
#define FUNCTIONS_H

/*
 * Pre: w is a valid integer greater than zero
 * Square: prints numbers up to w^2 in a square shape
 * Spiral: prints numbers up to w^2 in a spiral shape
 * Rotation: prints the numbers up to w^2 in a rotated shape
 */

void square (int w);
void spiral (int w);
void rotation (int w);

#endif
